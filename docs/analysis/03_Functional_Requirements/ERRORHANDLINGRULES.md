* If Gemini API fails, a retry attempt occurs.

* If retry fails, system shows a clear error.

* If student submission is empty, system prevents submission.

* If teacher reference answer is missing, question cannot be published.

* When network issues occur, UI shows non-technical message.